@* Require authorization for all pages in this directory and its subdirectories. *@
@attribute [Authorize(Policy = "ClubIsNotOnboarded")]
@*Do not assign layout here/in the same folder as the layout, as this will lead to a self-reference.*@
